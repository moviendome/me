---
title: Blog | Ruby/Ruby on Rails Engineer based in Bangkok, Thailand
pageable: true
per_page: 100
---
<div class="flex flex-col min-h-screen justify-center items-center m-10">
  <%= partial "header" %>

  <div class="grid max-w-4xl text-center">
    <div class="bg-gray-50 dark:bg-gray-800 border border-gray-200 dark:border-gray-700 rounded-lg p-8 md:p-12">
      <% page_articles.each do |article| %>

        <% next if article.tags.include?("draft") %>
        <!--
        <% article.tags.each do |tag| %>
          <span class=""><%= tag %></span>
        <% end %>
        -->

        <p class="text-xs font-normal text-gray-500 dark:text-gray-400">
          <time><%= article.date.strftime('%b %e, %Y') %>
        </p>

        <h2 class="text-gray-900 dark:text-gray-200 mb-10">
          <a href="<%= article.url %>"><%= article.title %></a>
        </h2>
      <% end %>
    </div>

    <% if paginate %>
      <nav class="pagination is-centered" role="navigation" aria-label="pagination">
        <ul class="pagination-list">
          <% if prev_page %>
            <%= link_to 'Previous', prev_page, class: "pagination-previous" %>
          <% end %>
          <% if next_page %>
            <%= link_to 'Next page', next_page, class: "pagination-next" %>
          <% end %>
        </ul>
      </nav>
    <% end %>
  </div>

  <%= partial "links" %>
</div>

